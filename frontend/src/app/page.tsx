/*
This file renders the public landing page route in Next.js.
Edit it when the home page should point to different content.
Copy it when you add another simple page route.
*/

import { LandingPage } from "../features/landing/LandingPage";

export default function Page() {
  return <LandingPage />;
}
